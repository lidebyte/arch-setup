
- base 

    1. 更改全局/etc/environment设置默认编辑器（默认使用vim，如果用户没有安装则安装，如果用户已经安装了nvim或者nano则设置为nano）
    2. 检测是否开启32位源,没有的话开启
    3. 安装基础字体wqy-zenhei noto-fonts noto-fonts-emoji
    4. 添加archlinuxcn源，并同步数据库和安装密钥
    5. 添加archlinuxcn之后可以直接pacman安装aur助手，yay和paru都装上吧
    6. 

- basic

    1. 检测是否为btrfs文件系统，如果是的话安装快照程序snapper、btrfs-assistant。检测是否使用grub，如果使用的话配置自动生成快照启动项和覆盖文件系统（这里需要重启，但是先不重启，继续接下去的操作）
    2. 安装音视频固件和服务
    3. 安装中文输入法
    4. 蓝牙（使用blueman就好了）
    5. 性能模式切换
    6. user-dir

- niri 

    1. 安装niri和重要程序
    2. 安装和配置图形化文档管理器
    3. 安装软件商城
    4. 安装所有我的仓库文件需要的依赖软件,我新增了一个niri-applist.txt文件，和intsall.sh在同目录下，里面是我的配置文件需要的所有依赖。需要用aur助手安装。用yay。
    5. 克隆我的dotfiles，https://github.com/SHORiN-KiWATA/ShorinArchExperience-ArchlinuxGuide.git。所有的配置文件，主题文件，vim配置，zsh配置之类的都在这个仓库的dotfiles/里面。这个dotfiles/相当于home/，移动进去。
    6. 我的仓库里还有一个wallpapers文件夹，把他移动到图片文件夹下更名为Wallpapers。
    7. ddcutil配置
    8. swayosd配置
    9. 自动登录

- 国区特供

    1. 安装时自动配置github镜像站，安装完成后恢复
    2. 


- issue